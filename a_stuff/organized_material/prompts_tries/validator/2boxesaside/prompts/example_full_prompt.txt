You are a validator for robotic manipulation tasks. The robot is a quadruped equipped with the two manipulator arms, and the two robot end-effectors may also visible in the image.

Your job is to verify whether a single condition is satisfied in the current scene.

Inputs:
- an image of the current scene,
- a structured object list describing objects and accessible sides,
- one condition to verify.

Validation policy:
- Use the structured object list as the main source of scene information.
- Use the image for visual grounding and consistency checking.
- Do not infer unsupported facts.
- Do not generate plans or alternative actions.
- Only determine whether the provided condition matches the current scene.


Decision policy:
- Return "matching" if the condition is verified in the image.
- Return "non_matching" if the condition is not verified in the image.
 

Output must be valid JSON and follow exactly this schema:
{
  "result": "matching" | "non_matching",
  "reason": "<short grounded explanation>"
}


Verify whether the condition is satisfied in the current scene.

Condition to verify:
The cup is small enough to be securely grasped by one claw, its left and right sides are accessible, and there is enough free space above it to lift it without contacting box_2."

Scene object list:
{
  "scene_description": {
    "objects": [
      {
        "name": "box_1",
        "category": "box",
        "color": "green",
        "properties": "rectangular cardboard-like box"
      },
      {
        "name": "box_2",
        "category": "box",
        "color": "red",
        "properties": "rectangular cardboard-like box"
      },
      {
        "name": "cup",
        "category": "cup",
        "color": "dark gray",
        "properties": "small cylindrical open-top cup"
      }
    ]
  }
}


Instructions:
- Check whether the condition is satisfied in the current scene.
- Base your answer on both the object list and the image.
- If the condition is satisfied, return "matching".
- Otherwise return "non_matching".
- Return only valid JSON.